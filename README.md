# Vacuum Cleaner AI Agent

NUM AI

I know how to use git

- Environment
  - 2d space
  - each cell = dirty || !dirty

- Perception
  - current location
  - isCurrentLocation = dirty || !dirty

- Actions
  - Suck
  - Move
  - NoOp

- Agent Function
  - if currentLoc == "dirty"
    - suck()
  - else
    - move()
